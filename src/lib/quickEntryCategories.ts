// Hardcoded common categories -- the real Category system doesn't exist
// until a later prompt. The selected value is stored as plain text on
// TransactionEntry.categoryId for now (see Prompt 5's schema note).
export const INCOME_CATEGORIES = ["Salary", "Business Sale", "Freelance", "Gift", "Other"];
export const EXPENSE_CATEGORIES = ["Food", "Transport", "Rent", "Utility Bill", "Shopping", "Other"];
